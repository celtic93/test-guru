<div class="row justify-content-center mt-3">
  <div class="col col-md-6"> 
    <h1><%= t('.header') %></h1>

    <table class="table table-striped">
      <tr>
        <th><%= t('.question') %></th>
        <th><%= t('.link') %></th>
        <th><%= t('.author') %></th>
      </tr>
      <%= render @gists %>
    </table>
  </div>
</div>
