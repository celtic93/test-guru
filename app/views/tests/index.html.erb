<div class="row justify-content-center mt-3">
  <div class="col col-md-6">
    <h1><%= t('.header') %></h1>

    <table class="table table-striped">
      <tr>
        <th><%= t('.title') %></th>
        <th><%= t('.level') %></th>
        <th><%= t('.questions') %></th>
        <th><%= t('.actions') %></th>
      </tr>

      <%= render @tests %>
    </table>
  </div>
</div>
